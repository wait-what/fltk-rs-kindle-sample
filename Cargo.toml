[package]
name = "kindle-fltk-rs"
version = "0.1.0"
edition = "2024"

[profile.release]
strip = true
panic = "abort"
lto = true
codegen-units = 1
opt-level = "z"

[dependencies]
fltk = { git = "https://github.com/wait-what/fltk-rs-kindle", features = ["no-pango"] }
