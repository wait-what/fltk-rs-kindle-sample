[package]
name = "sample"
version = "0.1.0"
edition = "2024"

[profile.release]
strip = true
panic = "abort"
lto = true
codegen-units = 1
opt-level = "z"

[dependencies]
fltk = { version = "1.5.22", features = ["no-pango"] }
